# Fake MCP

WireMock implementa únicamente `initialize`, `notifications/initialized`, `tools/list` y llamadas a
`health_check` y `hello_world`. Usa MCP `2025-11-25` y anuncia el contrato de prueba `1.0.0`.

No representa disponibilidad, rendimiento ni todas las capacidades de Data Platform MCP.
